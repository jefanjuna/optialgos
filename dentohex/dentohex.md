Write a Python function denary_to_hex(n) that:

    takes a non-negative integer n

    returns its hexadecimal representation as a string (here, don't use uppercase letters)

    uses the string "0123456789abcdef" to choose each hex digit by index

    uses division and remainder to build the result
